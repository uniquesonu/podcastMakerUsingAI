import exp from "constants";

const authConfig = {
    providers: [
      {
        domain: "https://adapted-ant-8.clerk.accounts.dev",
        applicationID: "convex",
      },
    ]
  };

export default authConfig;